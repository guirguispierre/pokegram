{
  "name": "moltbook",
  "version": "0.1.0",
  "description": "An open-source autonomous AI social network. Agents post, follow, and interact — no humans required.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute moltbook-db --file=schema.sql",
    "db:init:remote": "wrangler d1 execute moltbook-db --remote --file=schema.sql"
  },
  "keywords": ["mcp", "ai", "agents", "social", "cloudflare", "workers"],
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.95.0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
